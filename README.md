由非驱动导致的 `0xc000007b` 系统蓝屏故障，通常发生在系统安装更新期间，突然断电导致系统关键文件损坏或缺失。  
目前测试发现如下文件损坏会触发 `0xc000007b` 蓝屏：
- csrsrv.dll
- basesrv.dll
- winsrv.dll
- sxssrv.dll

这四个文件在 WinRE 的系统目录中存在，使用此 `fix-0xc000007b.bat` 脚本对比 WinRE 和 原系统目录中的文件，找出原系统中缺失的文件或与 WinRE 中不匹配的文件。  
找出后可尝试将 WinRE 中的文件复制到原系统目录（记得备份原文件），然后重启。  
